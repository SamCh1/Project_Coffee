extends ../../layouts/default.pug

block main
  .container.my-3
    .row
      .col-12


    .row
      .col-12
        div(class="alert alert-success")
          | Chúc mừng bạn đã đặt hàng thành công! Chúng tôi sẽ xử lý đơn hàng trong thời gian sớm nhất.

    if(order)
      div(class="cart")
        div(class="container")
          div(class="cart__title" style="color:#616161;")
          div(class="cart__table")
            h4(class="login__form-text") Thông tin cá nhân
            table(class="table table-bordered" table-cart)
              tbody
                tr
                  td Họ tên
                  td
                    b #{order.userInfo.fullName}
                tr
                  td Số điện thoại
                  td
                    b #{order.userInfo.phone}
                tr
                  td Địa chỉ
                  td
                    b #{order.userInfo.address}

    div(class="cart")
      div(class="container")
        div(class="cart__title" style="color:#616161;")
          h4(class="login__form-text") Thông tin đơn hàng
        div(class="cart__table")
          table(class="table table-bordered" table-cart)
              thead
                tr
                  th STT
                  th Ảnh
                  th Tên
                  th Giá
                  th Số lượng
                  th Tổng tiền
              tbody
                if(order.products.length > 0)
                  each item, index in order.products
                    tr
                      td #{index + 1}
                      td
                        img(
                          src=item.thumbnail
                          alt=item.title
                          width="80px"
                        )
                      td #{item.title}
                      td #{item.price} VND
                      td #{item.quantity}
                      td #{item.totalPrice} VND
          if(order.products.length > 0)
              h3(class="text-right") Tổng đơn hàng: #{order.totalPrice} VND